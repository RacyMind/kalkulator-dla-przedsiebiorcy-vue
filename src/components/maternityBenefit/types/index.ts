export enum EmploymentType {
  EmploymentContract = 'employmentContract',
  SelfEmployment = 'selfEmployment',
}

export enum ZusType {
  Big = 'big',
  Preferential = 'preferential',
  Custom = 'custom',
}

export type ChildrenCount = 1 | 2 | 3 | 4 | 5
