# Contracts: 021-real-cost-display

No API contracts needed — this feature modifies only frontend calculator logic and UI display. The "contract" is the `Result` interface which is documented in `data-model.md`.
